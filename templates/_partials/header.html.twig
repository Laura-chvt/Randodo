<nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_hike_index') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Randodo Logo" class="nav-logo">
        </a>
        {% if app.user %}
        <div class="ms-auto">
            <a href="{{ path('app_user_show', {id: app.user.id}) }}" class="btn btn-outline-brown rounded-pill px-4">Mon Compte</a>
        </div>
        <div class="ms-auto">
            <a href="{{ path('app_logout') }}" class="btn btn-outline-brown rounded-pill px-4">Se déconnecter</a>
        </div>
        {% else %}
        <div class="ms-auto">
            <a href="{{ path('app_register') }}" class="btn btn-outline-brown rounded-pill px-4">S'inscrire</a>
        </div>
        <div class="ms-auto">
            <a href="{{ path('app_login') }}" class="btn btn-outline-brown rounded-pill px-4">Se connecter</a>
        </div>
        {% endif %}
        {% if is_granted('ROLE_MODO') %}
        <div class="ms-auto">
            <a href="{{ path('app_hike_create') }}" class="btn btn-outline-brown rounded-pill px-4">Ajouter une Randonnée</a>
        </div>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
        <div class="ms-auto">
            <a href="{{ path('app_user_index') }}" class="btn btn-outline-brown rounded-pill px-4">Dashboard</a>
        </div>
        {% endif %}
    </div>
</nav>